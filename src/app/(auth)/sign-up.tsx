import { View, Text } from 'react-native';

export default function SignUpScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-gray-100 dark:bg-navy">
      <Text className="text-lg font-semibold text-navy dark:text-mist">Sign Up</Text>
    </View>
  );
}
