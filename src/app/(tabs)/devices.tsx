import { Text, View } from 'react-native';

export default function DevicesScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-gray-100 dark:bg-navy">
      <Text className="text-xl font-semibold text-navy dark:text-mist">Devices</Text>
    </View>
  );
}
