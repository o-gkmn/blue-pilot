import { Text, View } from "react-native";

export default function DevicesScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-xl font-semibold text-text">Devices</Text>
    </View>
  );
}
